/**
 * Babel: Converter (transpilar) código do React para um código que o browser entenda
 * Webpack: Pra cada tipo de arquivo (js, css,png) eu vou converter ocódigo de uma maneira diferente
 * 
 * Loaders:  Babel-loader, css-loader, image-loader 
 * Terminações "LOADER": (serve para arquivos css etc que não são lidos pelo babel)
 * 
 * Babel:"Preset-env"= converte o ambiente / "preset-react =converte o html para o js"
 */